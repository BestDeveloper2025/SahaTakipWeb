version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Tarayıcının API istekleri (Nginx üzerinden aynı host)
        VITE_API_BASE: http://77.92.152.65
    container_name: sahatakip-web
    restart: unless-stopped
    ports:
      - "80:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
